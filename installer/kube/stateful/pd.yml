apiVersion: v1
kind: Service
metadata:
  name: pd
  labels:
    app: pd
spec:
  ports:
  - port: 2380
    name: peer
  - port: 2379
    name: client
  clusterIP: None
  selector:
    app: pd
---
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: pd
spec:
  selector:
    matchLabels:
      app: pd # has to match .spec.template.metadata.labels
  serviceName: "pd"
  replicas: 3 # by default is 1
  template:
    metadata:
      labels:
        app: pd # has to match .spec.selector.matchLabels
        policy: egress-all
    spec:
      terminationGracePeriodSeconds: 10
      containers:
      - name: pd
        image: tidb/installer-centos:7
        imagePullPolicy: "IfNotPresent"
        command: ["/usr/sbin/init"]
        securityContext:
          privileged: true
        ports:
        - containerPort: 2380
          name: peer
        - containerPort: 2379
          name: client
        volumeMounts:
        - name: data1
          mountPath: /data1
      volumes:
        - name: cgroup
          hostPath:
            path: /sys/fs/cgroup
            type: Directory
  volumeClaimTemplates:
  - metadata:
      name: data1
    spec:
      accessModes: [ "ReadWriteMany" ]
      storageClassName: "standard"
      resources:
        requests:
          storage: 1Gi
